'use strict';

var _tokenize = require('./tokenize');

var _tokenize2 = _interopRequireDefault(_tokenize);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('Annotator', function () {
  var annotator = void 0;

  describe('TokenizerAnnotator', function () {
    beforeEach(function () {
      annotator = new _tokenize2.default();
    });

    it('should have a proper pipeline', function () {
      expect(annotator.pipeline()).to.deep.equal(['tokenize']);
    });

    it('should have the proper default options', function () {
      expect(annotator.options()).to.deep.equal({
        language: 'Unspecified',
        // class: null,
        whitespace: false,
        keepeol: false,
        // options: null,
        verbose: false
      });
    });
  });
});