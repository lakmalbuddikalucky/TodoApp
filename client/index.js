import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App';

// This will get the main component APP and render it on the HTML using ReactDOM
ReactDOM.render(<App/>,
	document.getElementById('app'));
